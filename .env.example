# ZenML Enterprise MLOps - Environment Variables
# Copy this file to .env and fill in your values
#
# 2-Workspace Architecture:
# - enterprise-dev-staging: Development + Staging (full lineage)
# - enterprise-production: Production only (imported models)
#
# TWO WAYS TO CONNECT:
#
# Option 1 (Interactive - best for local dev):
#   zenml login <workspace-name> --api-key
#   # You'll be prompted for your API key
#
# Option 2 (Environment variables - best for CI/CD):
#   Set ZENML_STORE_URL and ZENML_STORE_API_KEY below.
#   ZenML will automatically connect using these environment variables.

# =============================================================================
# Active Workspace Connection (set to whichever workspace you're working with)
# =============================================================================
ZENML_STORE_URL=https://your-workspace.zenml.io
ZENML_STORE_API_KEY=your-api-key

# =============================================================================
# Dev-Staging Workspace Credentials (for cross-workspace scripts)
# =============================================================================
ZENML_DEV_STAGING_STORE_URL=https://your-dev-staging.zenml.io
ZENML_DEV_STAGING_API_KEY=your-dev-staging-api-key

# =============================================================================
# Production Workspace Credentials (for cross-workspace scripts)
# =============================================================================
ZENML_PRODUCTION_STORE_URL=https://your-production.zenml.io
ZENML_PRODUCTION_API_KEY=your-production-api-key

# =============================================================================
# GCP Configuration (single project with labels for cost tracking)
# =============================================================================
GCP_PROJECT_ID=zenml-core
GCP_REGION=us-central1

# =============================================================================
# ZenML Project (same in both workspaces)
# =============================================================================
ZENML_PROJECT=cancer-detection

# =============================================================================
# Stack Names
# =============================================================================
# Dev-Staging workspace stacks:
ZENML_DEV_STACK=dev-stack
ZENML_STAGING_STACK=staging-stack

# Production workspace stack:
ZENML_PRODUCTION_STACK=gcp-stack

# =============================================================================
# Model Exchange Bucket (for cross-workspace promotion)
# =============================================================================
MODEL_EXCHANGE_BUCKET=zenml-core-model-exchange
