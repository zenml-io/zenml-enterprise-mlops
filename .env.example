# ZenML Enterprise MLOps - Environment Variables
#
# SIMPLE SETUP:
# 1. Copy this to .env: cp .env.example .env
# 2. Fill in your TWO sets of workspace credentials below
# 3. Switch workspaces: source scripts/use-workspace.sh dev-staging
#                   or: source scripts/use-workspace.sh production
#
# =============================================================================
# DEV-STAGING WORKSPACE (training, staging validation)
# =============================================================================
ZENML_DEV_STAGING_URL=https://your-dev-staging.zenml.io
ZENML_DEV_STAGING_API_KEY=ZENKEY_your_dev_staging_key

# =============================================================================
# PRODUCTION WORKSPACE (production inference only)
# =============================================================================
ZENML_PRODUCTION_URL=https://your-production.zenml.io
ZENML_PRODUCTION_API_KEY=ZENKEY_your_production_key

# =============================================================================
# GCP Configuration
# =============================================================================
GCP_PROJECT_ID=zenml-core
GCP_REGION=us-central1

# =============================================================================
# ZenML Project (same name in both workspaces)
# =============================================================================
ZENML_PROJECT=cancer-detection

# =============================================================================
# Stack Names
# =============================================================================
ZENML_DEV_STACK=dev-stack
ZENML_STAGING_STACK=staging-stack
ZENML_PRODUCTION_STACK=gcp-stack

# =============================================================================
# Model Exchange Bucket
# =============================================================================
MODEL_EXCHANGE_BUCKET=zenml-core-model-exchange
